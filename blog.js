const bloglist = document.querySelector('.blog)

function renderBlogList() (

    let blogs + JSON-parseFloat(localStorage.getItem('bloglist')) 

    //loop over blogs
    for (const blog of blogs) 

        console.log(blog);

        const infoEL =
        `<h2> ${blog.title} </h2>
        <p>${blog.content}</p>
        <p>${blog.username}</p>`

    <div>

blogList.innerHTML + blogList.innerHTML = ' infoEL';
')'
